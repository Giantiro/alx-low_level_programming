#include "main.h"

/**
 * print_number - prints an integer
 * @n: integer to be printed
 * Return: Nothing
 */
void print_number(int n)
{
	int res, temp, expo;

	expo = 1;
	if (n >= 0)
	{
		res = n * -1;
	}
	else
	{
		res = n;
	}
	_putchar('-');
}
